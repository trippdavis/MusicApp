<h2><%= @band.name %></h2>
<h3><%= @album.title %>[<%= @album.recording_type %>]</h3>

Tracks:
<ul>
  <% @album.tracks.each do |track| %>
    <li>
      <%= link_to track.title, track_url(track) %>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to "Edit Album", edit_album_url(@album) %>
  <%= link_to "Delete Album", album_url(@album), method: :delete %>
</p>

<p>
  <%= link_to "Add a Track", new_album_track_url(@album) %>
</p>

<p>
  <%= link_to "Back to Artist", band_url(@band) %>
</p>
